# ─── Desenvolvimento local ────────────────────────────────────────────────────
# O Vite faz proxy de /api/* para VITE_BACKEND_URL, então o axios chama /api/*
# e não precisa conhecer a porta do backend diretamente.
VITE_API_URL=/api
VITE_BACKEND_URL=http://127.0.0.1:8000
